# Docker Setup for Football Time Comparator

## Prerequisites
- Docker and Docker Compose installed
- Telegram bot token and chat ID configured

## Quick Start

1. **Build the Docker image**
   ```bash
   npm run docker:build
   # or
   make build